# RIG Wire — whole-app audit (2026-06-21)

Automated sweep: tsc, all routes, API error paths, live data quality, gen/pipeline health, dedup. Numbers
measured on the live DB + dev server (needs SSH tunnel :5433).

## Severity summary
| # | Finding | Severity | Owner |
|---|---|---|---|
| 1 | Titleless headlines on **340/2,697 surfaceable (12.6%)** — "**"/empty headlines render as broken cards | HIGH | DB (title extraction) |
| 2 | `/long-read` has **11 dead `href="#"` links** (in the hardcoded Intelligence/Podcast bands) | HIGH | me |
| 3 | **3 hardcoded bands** (Editorial, rw Intelligence, Podcasts) = fake content interleaved with real | HIGH | me (hide) |
| 4 | Footer "Back to today's edition" → `/today` is **auth-gated** → logged-out users bounce to /signin | MED | me |
| 5 | Articles thin (hedge "single source") | MED | DB (deepen facts) |
| 6 | 🔑 Key rotation (Groq+Cerebras) + deploy-with-DB = go-public blockers | BLOCKER | user |

## What's healthy ✅
- **tsc clean. All routes HTTP 200.**
- **Content-gen is LIVE** — 3,615 articles in last 24h, 759 in last 6h (latest 22:51). Keys/pipeline NOT exhausted.
- **API error handling solid:** `/api/worldwide?scope=world` 200+envelope · bad scope `ZZ9` → **400**+envelope ·
  `/api/worldwide/shown` 200 · bad slug → **404** · ghost UUID → **404**.
- **Surfaceable degenerate = 0** — no "no facts available" garbage reaches the page (guard + gen fix hold).
- **Dedup working** — 94 `redirected_to` + 204 `suppressed` clusters exist; the page guard excludes them.
- **Fresh + diverse** (today's fixes): top 4–7h, max top-8 age 17.3h, mega-events collapse to one card.
- Published: 2,709 (source-grounded 2,029 / hybrid 680). Surfaceable+articled: 2,697; within 72h: 527; <24h: 108.

## Detail by area

### Routes / auth surface
Public = `/` (landing, 127KB, real) + `/long-read` (+scope) + `/signin`. **All other modes — `/today`,
`/digest`, `/minute`, `/queue`, `/all-sides`, `/long-view`, `/onboarding` — redirect to `/signin`** (each
returns the ~40.8KB signin page). They're static prototype, intentionally gated. Consequence: the Worldwide
footer link to `/today` dead-ends at the auth wall for logged-out visitors (Finding 4).

### Worldwide page (the live product)
- Live + working: Top Stories, More Top Stories, topic bands, Around the World, LiveNewsRail (real), Most
  covered (real), scope filter, story reads. No degenerate, dedup-safe, fresh.
- Broken: **11 dead `#` links** + **3 fake bands** (Findings 2–3) — both fixed by hiding the 3 hardcoded bands.
- **Titleless "**" headlines (12.6%)** leak onto real cards (Finding 1) — bigger than the earlier ~2.5%
  estimate; DB title-extraction gap.

### Content / data quality
- Gen live and fresh-first; degenerate 0; thin reads (Finding 5, DB deepening facts in progress).
- Single-source clusters still 0 facts → single-article synthesis still not viable (known).

## Recommended actions
1. **Me (now):** hide the 3 hardcoded bands → removes Findings 2 + 3 in one change. Fix footer link (Finding 4)
   → point to `/long-read` or a public route.
2. **DB chat:** title-extraction fix for the 12.6% titleless (Finding 1) — biggest visible quality gap; plus
   continue fact-deepening (Finding 5).
3. **User:** key rotation + deploy (Finding 6) before public.

## Not covered (needs visual/manual)
Mobile/responsive layout, real broken-image rate (picsum static-band fallback availability), dark-mode,
per-component pixel polish — screenshot tooling was flaky this session; recommend a manual pass on iPhone-12 +
desktop before launch.
