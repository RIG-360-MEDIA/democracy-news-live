import { QueuePage } from '@/components/queue/queue-page';

export const metadata = {
  title:       'The Queue — Rig Wire',
  description: 'Press play. The Queue takes over. Your personal audio news stream.',
};

export default function Page() {
  return <QueuePage />;
}
