import { MinutePage } from '@/components/minute/minute-page';

export const metadata = {
  title:       'The Minute — Rig Wire',
  description: 'The world in 60 seconds. Today’s headlines, summarised.',
};

export default function Page() {
  return <MinutePage />;
}
