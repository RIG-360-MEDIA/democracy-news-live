import { LongViewPage } from '@/components/long-view/long-view-page';

export const metadata = {
  title:       'The Long View — Rig Wire',
  description: 'A reporter’s notebook of long-range analysis. What we got right. What we missed.',
};

export default function Page() {
  return <LongViewPage />;
}
