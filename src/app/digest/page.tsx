import { DigestPage } from '@/components/digest/digest-page';

export const metadata = {
  title:       'The Digest — Rig Wire',
  description: 'Five stories. Five minutes. Built around what you actually read for.',
};

export default function Page() {
  return <DigestPage />;
}
