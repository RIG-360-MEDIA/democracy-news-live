import { AllSidesPage } from '@/components/all-sides/all-sides-page';

export const metadata = {
  title:       'All Sides — Rig Wire',
  description: 'Every side of every story. Bias-balanced aggregation across the political spectrum.',
};

export default function Page() {
  return <AllSidesPage />;
}
